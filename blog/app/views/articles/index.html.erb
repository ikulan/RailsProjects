<h1>Articles </h1>

Our blog has <%= @articles.count %> articles and counting!

<ul class="max-w-md divide-y divide-gray-200 ">
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li class="">
        <div class="flex items-center space-x-4">
          <div><%= link_to article.title, article %></div>
          <% if article.draft? %>
            <div class="rounded-full text-sm px-5 py-2.5 text-center me-2 mb-2 bg-gray-100 text-gray-900"> Draft </div>
          <% elsif article.scheduled? %>
            <div class="rounded-full text-sm px-5 py-2.5 text-center me-2 mb-2 bg-green-100 text-green-900"> Scheduled </div>
          <% end %>
        </div>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>

