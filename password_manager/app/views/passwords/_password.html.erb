<%= link_to password, id: dom_id(password), class: "p-4 block no-underline rounded hover:bg-gray-100" do %>
  <div><%= password.url %></div>
  <div class="text-sm text-gray-500 font-normal"><%= password.username %></div>
<% end %>